import { FC } from 'react';

import Statistics from '../../../components/layout/statistics';

const Main: FC = () => {
  return (
    <section>
      <section>
        <Statistics />
      </section>
    </section>
  );
};

export default Main;
